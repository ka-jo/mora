import type { Scope } from "@/Scope";

/**
 * Internal helpers to manage the active scope stack. Implementation pending.
 *
 * @internal
 */
export function pushActiveScope(_scope: Scope): void {
	// no-op placeholder; real implementation will manage a stack
}

/**
 * @internal
 */
export function popActiveScope(): void {
	// no-op placeholder
}
